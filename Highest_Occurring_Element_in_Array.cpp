#include<bits/stdc++.h>
using namespace std;

int main(){
    // input an array
    int n;
    cout<<"enter the number of element in the array :";
    cin>>n;
    int arr[n]={};
    for (int i=0;i<n;i++){
        cin>>arr[i];
    }

    // precompute


    // find the highest frequency occuring element

    

    return 0;
}